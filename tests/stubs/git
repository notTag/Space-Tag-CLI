#!/bin/sh

echo "git $*" >> "${STUB_LOG:?}"
if [ "${1:-}" = rev-parse ]; then
  if [ -n "${STUB_GIT_ROOT:-}" ]; then
    echo "$STUB_GIT_ROOT"
    exit 0
  fi
  echo "fatal: not a git repository" >&2
  exit 128
fi
exit 0
